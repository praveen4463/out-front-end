// TODO:
// put these in sorted
// these are incomplete, complete them after api is modified to fix a few
// functions.
// Also we need to implment suggestions for available constants like keys, colors...
const all = [
  'assertTrue(expression)',
  'assertFalse(expression)',
  'assertThrows(expression)',
  'assertDoesNotThrow(expression)',
  'containsElement(el)',
  'containsKey(key)',
];

export default all;
