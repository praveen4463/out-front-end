// TODO:
// put these in sorted
// these are incomplete, complete them after api is modified to fix a few
// functions.
const all = [
  'openUrl(url)',
  'getCurrentUrl()',
  'back(n)',
  'forward(n)',
  'refresh()',
  'getTitle()',
  'openUrlNewWin(url, winType)',
  'findElement(using, by, noWait)',
  'findElements(using, by, noWait)',
  'findElementFromElement(elemId|selector, using, by, noWait)',
  'findElementsFromElement(elemId|selector, using, by, noWait)',
  'click(elemId|selector)',
];

export default all;
